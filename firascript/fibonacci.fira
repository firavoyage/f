fib (n)
  memo = {}
  f(i)
    i in memo ? memo[i] // in(a,b) b.hasOwnProperty(a)
    i <= 2 ? 1
    memo[i] = f(i - 1) + f(i - 2)
  f(n)

pr fib(10) // 55

// (a b c d) <=> a(b,c,d)
pr(fib 10) // 55

pr fib 10 // (){}, 10