app:
  sidebar:
  main:
  command palette: # should have
sidebar:
  alt bar: # could have
    search: 
      search input: # wont have, call command palette instead
    app name: Tools # title case
    breadcrumb:
  list: items (headings and items), tool
  to resize: # could have, when hovered
main:
  title bar: # could have
    sidebar toggle:
    title: # title case
    window actions: # wont have (in browser)
      minify:
      maximize:
      close:
  panels:
    args/recipe: # should have
    editor.input: tool, input
    editor.output: tool, input # output doesnt have to be a global state
    to resize: # could have
editor: 
  tab bar:
    title:
    copy: # could have
      to turn it off: # should have
    args: # could have
  textarea:
    code editor: # could have

