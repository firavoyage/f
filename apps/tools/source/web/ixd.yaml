# m = must have, s = should have, c = could have, w = wont have
app:
  sidebar:
  main:
  command palette: # s
  shortcuts:
  settings:
  about:
sidebar:
  menu: # c
  # alt bar: # c
    search: 
      search input: # w, call command palette instead
    app name: Tools # title case
    hamburger: # alt bar
  list: items (headings and items), tool
  to resize: # c, when hovered
main:
  title bar: # c
    sidebar toggle:
    title: # title case
    window actions: # w (in browser)
      minify:
      maximize:
      close:
  workspace:
    # args/recipe: # s
    process: # m
      item:
        args:
        to delete: # s
        to reorder # c
      to clear: # s
      to copy link: # c
      to copy code: # c
      to import code: # c
      to edit code: # c
    input: tool, input
    output: tool, input # output doesnt have to be a global state
    to resize: # c
editor: 
  tab bar:
    title:
    copy: # c
      to turn it off: # s
    args: # c
  textarea:
    code editor: # c

