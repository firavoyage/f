sidebar:
  convert:
    json yaml toml xml:
      - json to yaml
      - json to toml
      - json to xml
      - yaml to json
      - yaml to toml
      - yaml to xml
      - toml to json
      - toml to yaml
      - toml to xml
      - xml to json
      - xml to yaml
      - xml to toml
    markdown html:
      - markdown to html
      - html to markdown
    timestamp utc: # i may rethink the label "utc" to match the broader term
      - timestamp to utc # arg: timezone (when to utc), fmt
      - utc to timestamp
    # color:
    #   - auto # from anything to all
    #   - hex
    #   - rgb
    #   - hsl
    #   - hsv
    #   - cmyk
    #   - oklch
  encode/decode:
    - base64
    - url
    - text
  encrypt/decrypt:
    - gpg
  hash:
    - sha
    - md5
  style:
    - leet speak
  lookup:
    - unicode characters # symbols and emojis
    - en dict
    - zhcn society id geolocation
    - university ranking
  calculate:
    - color contrast:
      - perceptual # apca, Advanced Perceptual Contrast Algorithm
      - relative # wcag 2, Relative Luminance Contrast Ratio, inaccurate/approximate
      - binary # yiq, Luma (Y) Channel Extraction
    - maimai:
      - loss of each note
      - achivement rate to perf
    # - article:
    #   - word count
    #   - typos
  generate:
    - lorem ipsum
  # misc:
  #   - regex
  #   - grep
  #   - glob
command palette:
  toggle sidebar:
  open command palette:
  open keyboard shortcuts:
  open settings:
  
# - data format
#   - url decode <!-- e.g. %3d becomes = -->
# - language
#   - language decode <!-- yes, it's vscode builtin, but why not -->
#   - leet speak
# - date/time
#   - timestamp, utc, timezone, (any template string)
# - encryption, public key, hash
#   - (popular methods)
# - misc
#   - regex (the args are defined separately from inputs, on the recipe)
#   - grep
#   - glob
#   - lorem ipsum

# - planned
#   - maimai
#   - color contrast
#   - json yaml toml
#   - base64
#   - unicode and emojis. aliases. synonyms. vector semantic search. <!-- it doesnt have to be a converter. if you feel it would be a tool, then it is a tool. -->
#   - dict (auto correct, string distance)
#   - typo check
#   - word counter

# - lookup
#   - chinese id (or prefix) to geolocation
#   - university ranking
