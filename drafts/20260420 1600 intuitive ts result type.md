<html><head></head><body><section dir="auto" style="color: rgb(0, 0, 0); white-space-collapse: collapse;"><div><div><div><div dir="auto"><div><div><div>how could i fix it. - make ok exclude err - let it log err instead of an empty object<pre><code>type ok&lt;T&gt; = Exclude&lt;T, err&gt;
type err = { type: string, message: string }

declare global {
  type result&lt;T&gt; = ok&lt;T&gt; | err;
}

export function ok&lt;T&gt;(value: T): ok&lt;T&gt; {
  return value
}

const err_prototype = {}

export function err(error: err): err {
  return Object.create(err_prototype, {
    type: {
      value: error.type,
      writable: false,
    },
    message: {
      value: error.message,
      writable: false,
    }
  })
}

export function rescue&lt;T&gt;(result: result&lt;T&gt;): boolean {
  return result instanceof err
}

function foo(): result&lt;boolean&gt; {
  if (Math.random() &gt; 0.5 &amp;&amp; false) {
    return ok(true)
  } else {
    return err({ type: "bad luck", message: "damn" })
  }
}

function test() {
  let bar = foo()

  if (rescue(bar)) {
    console.log(bar);
    return;
  }

  console.log(bar);
}

test()</code></pre></div></div></div></div></div><div></div></div></div></section><section dir="auto" style="color: rgb(0, 0, 0); white-space-collapse: collapse;"><div><div><div><div dir="auto"><div><div><p>You’re very close, but there are two core problems in your design:</p><ol><li><code>Exclude&lt;T, err&gt;</code> doesn’t actually prevent <code>T</code> from being an <code>err</code></li><li><code>instanceof err</code> can never work because <code>err</code> is <strong>not a class</strong></li></ol><p>Let’s fix both cleanly.</p><hr><h2><span role="text">1. <code>ok&lt;T&gt;</code> doesn’t exclude <code>err</code></span></h2><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>ok</span>&lt;<span>T</span>&gt; <span>=</span> <span>Exclude</span>&lt;<span>T</span>, <span>err</span>&gt;</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>This only removes <code>err</code> <strong>if <code>T</code> is already a union containing it</strong>, which isn’t your case. So it doesn’t protect anything.</p><p>👉 There’s no reliable way to “forbid” a structural type like <code>{ type: string; message: string }</code> in TypeScript.</p><p><strong>Better approach:</strong> don’t fight the type system—use a <strong>tagged union</strong>.</p><hr><h2><span role="text">2. <code>instanceof err</code> is broken</span></h2><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>rescue</span>&lt;<span>T</span>&gt;(<span>result</span>: <span>result</span>&lt;<span>T</span>&gt;): <span>boolean</span> {<br>  <span>return</span> <span>result</span> <span>instanceof</span> <span>err</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p><code>err</code> is a <strong>type + function</strong>, not a constructor. So <code>instanceof</code> always fails.</p><p>Also, your <code>Object.create(err_prototype, …)</code> gives you an object with a custom prototype—but you're not checking against that prototype.</p><hr><h2>Fix: Use a discriminated union (idiomatic + safe)</h2><p>This is the standard pattern and solves everything cleanly.</p><h3>Define types</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>Ok</span>&lt;<span>T</span>&gt; <span>=</span> { ok: <span>true</span>; value: <span>T</span> }<br><span>type</span> <span>Err</span> <span>=</span> { ok: <span>false</span>; type: <span>string</span>; message: <span>string</span> }<br><br><span>type</span> <span>Result</span>&lt;<span>T</span>&gt; <span>=</span> <span>Ok</span>&lt;<span>T</span>&gt; <span>|</span> <span>Err</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h3>Constructors</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>ok</span>&lt;<span>T</span>&gt;(<span>value</span>: <span>T</span>): <span>Ok</span>&lt;<span>T</span>&gt; {<br>  <span>return</span> { ok: <span>true</span>, value }<br>}<br><br><span>export</span> <span>function</span> <span>err</span>(<span>error</span>: { type: <span>string</span>; message: <span>string</span> }): <span>Err</span> {<br>  <span>return</span> { ok: <span>false</span>, ...<span>error</span> }<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h3><span role="text">Type guard (your <code>rescue</code>)</span></h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>isErr</span>&lt;<span>T</span>&gt;(<span>result</span>: <span>Result</span>&lt;<span>T</span>&gt;): <span>result</span> <span>is</span> <span>Err</span> {<br>  <span>return</span> <span>result</span><span>.</span>ok <span>===</span> <span>false</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h3>Usage</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>function</span> <span>foo</span>(): <span>Result</span>&lt;<span>boolean</span>&gt; {<br>  <span>if</span> (<span>Math</span><span>.</span>random() <span>&gt;</span> <span>0.5</span>) {<br>    <span>return</span> <span>ok</span>(<span>true</span>)<br>  } <span>else</span> {<br>    <span>return</span> <span>err</span>({ type: <span>"bad luck"</span>, message: <span>"damn"</span> })<br>  }<br>}<br><br><span>function</span> <span>test</span>() {<br>  <span>const</span> <span>bar</span> <span>=</span> <span>foo</span>()<br><br>  <span>if</span> (<span>isErr</span>(<span>bar</span>)) {<br>    <span>console</span><span>.</span>log(<span>bar</span>) <span>// properly typed as Err</span><br>    <span>return</span><br>  }<br><br>  <span>console</span><span>.</span>log(<span>bar</span><span>.</span>value) <span>// properly typed as boolean</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2><span role="text">Why your original version logged <code>{}</code></span></h2><p>Because:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>console</span><span>.</span>log(<span>bar</span>)</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>When using <code>Object.create</code> with property descriptors, properties are <strong>non-enumerable by default</strong> unless you specify:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>enumerable</span>: <span>true</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>So your object <em>had properties</em>, but <code>console.log</code> didn’t show them.</p><hr><h2>If you really want to keep your prototype approach</h2><p>You’d need:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>err_prototype</span> <span>=</span> {}<br><br><span>export</span> <span>function</span> <span>err</span>(<span>error</span>: <span>err</span>): <span>err</span> {<br>  <span>return</span> <span>Object</span><span>.</span>create(<span>err_prototype</span>, {<br>    type: { value: <span>error</span><span>.</span>type, enumerable: <span>true</span> },<br>    message: { value: <span>error</span><span>.</span>message, enumerable: <span>true</span> }<br>  })<br>}<br><br><span>export</span> <span>function</span> <span>rescue</span>&lt;<span>T</span>&gt;(<span>result</span>: <span>result</span>&lt;<span>T</span>&gt;): <span>result</span> <span>is</span> <span>err</span> {<br>  <span>return</span> <span>Object</span><span>.</span>getPrototypeOf(<span>result</span>) <span>===</span> <span>err_prototype</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>But honestly—this is brittle and unnecessary.</p><hr><h2>Bottom line</h2><ul><li>❌ Don’t use <code>Exclude&lt;T, err&gt;</code> here</li><li>❌ Don’t use <code>instanceof</code> without a class</li><li>❌ Don’t rely on prototype tricks</li><li>✅ Use a <strong>discriminated union (<code>ok: true | false</code>)</strong></li><li>✅ Use a <strong>type guard</strong></li><li>✅ Keep it simple and predictable</li></ul><hr><p>If you want, I can <span role="button" tabindex="0"><span><svg xmlns="http://www.w3.org/2000/svg" width="20" height="20"></svg></span><span>show a version with <strong>pattern matching helpers</strong> (like Rust’s <code>match</code>) to make this even cleaner</span></span>.</p></div></div></div></div><div></div></div></div></section><section dir="auto" style="color: rgb(0, 0, 0); white-space-collapse: collapse;"><div><div><div><div dir="auto"><div><div><div>let the ok function exclude the type</div></div></div></div></div><div></div></div></div></section><section dir="auto" style="color: rgb(0, 0, 0); white-space-collapse: collapse;"><div><div><div><div dir="auto"><div><div><p>Short answer: you can’t <em>reliably</em> make <code>ok&lt;T&gt;</code> “exclude” <code>err</code> in the way you’re trying—at least not for a structural type like yours. But you can get <strong>close enough</strong> with a constraint that rejects anything <em>shaped like</em> <code>err</code>.</p><hr><h2>Why your current approach fails</h2><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>ok</span>&lt;<span>T</span>&gt; <span>=</span> <span>Exclude</span>&lt;<span>T</span>, <span>err</span>&gt;</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>This only removes <code>err</code> from a <strong>union</strong>, e.g.:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>A</span> <span>=</span> <span>Exclude</span>&lt;<span>string</span> <span>|</span> <span>err</span>, <span>err</span>&gt; <span>// string</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>But if someone passes a plain object that <em>looks like</em> <code>err</code>, TypeScript won’t stop them:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>ok</span>({ type: <span>"x"</span>, message: <span>"y"</span> }) <span>// still allowed ❌</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Because TypeScript is <strong>structural</strong>, not nominal.</p><hr><h2><span role="text">Practical workaround: forbid <code>err</code>-like shapes</span></h2><p>You can constrain <code>T</code> so it <strong>cannot extend <code>err</code></strong>:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>Err</span> <span>=</span> { type: <span>string</span>; message: <span>string</span> }<br><br><span>type</span> <span>NotErr</span>&lt;<span>T</span>&gt; <span>=</span> <span>T</span> <span>extends</span> <span>Err</span> <span>?</span> <span>never</span> <span>:</span> <span>T</span><br><br><span>export</span> <span>function</span> <span>ok</span>&lt;<span>T</span>&gt;(<span>value</span>: <span>NotErr</span>&lt;<span>T</span>&gt;): <span>T</span> {<br>  <span>return</span> <span>value</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2>What this gives you</h2><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>ok</span>(<span>true</span>) <span>// ✅</span><br><br><span>ok</span>({ foo: <span>123</span> }) <span>// ✅</span><br><br><span>ok</span>({ type: <span>"bad"</span>, message: <span>"oops"</span> }) <br><span>// ❌ Type error: resolves to never</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2>Limitation (important)</h2><p>This is <strong>not bulletproof</strong>. You can still bypass it:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>e</span>: <span>Err</span> <span>=</span> { type: <span>"x"</span>, message: <span>"y"</span> }<br><span>ok</span>(<span>e</span>) <span>// ❌ (correctly blocked)</span><br><br><span>const</span> <span>sneaky</span> <span>=</span> { type: <span>"x"</span>, message: <span>"y"</span> } <span>as</span> <span>any</span><br><span>ok</span>(<span>sneaky</span>) <span>// 😈 allowed (any bypasses everything)</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Also, this will reject <em>any</em> type that happens to match <code>{ type, message }</code>, even if it’s not your “error”.</p><hr><h2>Stronger pattern (if you really want enforcement)</h2><p>If you want something stricter, make <code>Err</code> <strong>nominal</strong>:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>declare</span> <span>const</span> <span>errBrand</span>: <span>unique</span> <span>symbol</span><br><br><span>type</span> <span>Err</span> <span>=</span> {<br>  type: <span>string</span><br>  message: <span>string</span><br>  [<span>errBrand</span>]: <span>true</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Now only your <code>err()</code> function can produce it:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>err</span>(<span>error</span>: { type: <span>string</span>; message: <span>string</span> }): <span>Err</span> {<br>  <span>return</span> {<br>    ...<span>error</span>,<br>    [<span>errBrand</span>]: <span>true</span><br>  }<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Then:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>NotErr</span>&lt;<span>T</span>&gt; <span>=</span> <span>T</span> <span>extends</span> <span>Err</span> <span>?</span> <span>never</span> <span>:</span> <span>T</span><br><br><span>export</span> <span>function</span> <span>ok</span>&lt;<span>T</span>&gt;(<span>value</span>: <span>NotErr</span>&lt;<span>T</span>&gt;): <span>T</span> {<br>  <span>return</span> <span>value</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Now it’s much harder to fake.</p><hr><h2>Reality check</h2><p>Even with all this, you’re still working <em>against</em> TypeScript’s design.</p><p>The idiomatic solution is still:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>Result</span>&lt;<span>T</span>&gt; <span>=</span><br>  <span>|</span> { ok: <span>true</span>; value: <span>T</span> }<br>  <span>|</span> { ok: <span>false</span>; error: <span>Err</span> }</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>It avoids all of this complexity entirely.</p><hr><p>If you want, I can show you a version that feels like Rust’s <code>Result</code> (with <code>.map</code>, <code>.unwrap</code>, etc.) but stays 100% type-safe.</p></div></div></div></div><div></div></div></div></section><section dir="auto" style="color: rgb(0, 0, 0); white-space-collapse: collapse;"><div><div><div><div dir="auto"><div><div><div>i mean in ok function, if it see T is actually err, throw. then T could not be err.</div></div></div></div></div><div></div></div></div></section><section dir="auto" style="color: rgb(0, 0, 0); white-space-collapse: collapse;"><div><div><div><div dir="auto"><div><div><p>What you’re asking for sounds reasonable at first, but there’s a mismatch between <strong>TypeScript’s compile-time types</strong> and <strong>runtime behavior</strong>:</p><blockquote><p>A generic like <code>T</code> does not exist at runtime, so <code>ok()</code> cannot “see” that <code>T</code> is <code>err</code> and throw based on that.</p></blockquote><p>So you can’t do this:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>function</span> <span>ok</span>&lt;<span>T</span>&gt;(<span>value</span>: <span>T</span>): <span>T</span> {<br>  <span>// ❌ impossible: T is erased at runtime</span><br>  <span>if</span> (<span>T</span> <span>===</span> <span>err</span>) <span>throw</span> ...<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2><span role="text">What you <em>can</em> do instead</span></h2><p>You have two separate goals:</p><ol><li><strong>Reject <code>err</code> at compile time</strong></li><li><strong>Guard at runtime just in case</strong></li></ol><p>You need to handle both explicitly.</p><hr><h2>1. Compile-time rejection (best effort)</h2><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>Err</span> <span>=</span> { type: <span>string</span>; message: <span>string</span> }<br><br><span>type</span> <span>NotErr</span>&lt;<span>T</span>&gt; <span>=</span> <span>T</span> <span>extends</span> <span>Err</span> <span>?</span> <span>never</span> <span>:</span> <span>T</span><br><br><span>export</span> <span>function</span> <span>ok</span>&lt;<span>T</span>&gt;(<span>value</span>: <span>NotErr</span>&lt;<span>T</span>&gt;): <span>T</span> {<br>  <span>return</span> <span>value</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>This makes TypeScript complain if someone <em>obviously</em> passes an <code>Err</code>.</p><hr><h2>2. Runtime check (what you actually asked for)</h2><p>Since <code>T</code> is gone at runtime, you must check the <strong>value shape</strong>, not the type:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>function</span> <span>isErrLike</span>(<span>value</span>: <span>any</span>): <span>value</span> <span>is</span> <span>Err</span> {<br>  <span>return</span> (<br>    <span>value</span> <span>&amp;&amp;</span><br>    <span>typeof</span> <span>value</span> <span>===</span> <span>"object"</span> <span>&amp;&amp;</span><br>    <span>typeof</span> <span>value</span><span>.</span>type <span>===</span> <span>"string"</span> <span>&amp;&amp;</span><br>    <span>typeof</span> <span>value</span><span>.</span>message <span>===</span> <span>"string"</span><br>  )<br>}<br><br><span>export</span> <span>function</span> <span>ok</span>&lt;<span>T</span>&gt;(<span>value</span>: <span>NotErr</span>&lt;<span>T</span>&gt;): <span>T</span> {<br>  <span>if</span> (<span>isErrLike</span>(<span>value</span>)) {<br>    <span>throw</span> <span>new</span> <span>Error</span>(<span>"ok() received an Err"</span>)<br>  }<br>  <span>return</span> <span>value</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2>⚠️ Important caveat</h2><p>This will also throw for <em>any object that happens to look like</em>:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div>{ <span>type</span>: <span>string</span>; <span>message</span>: <span>string</span> }</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>So something unrelated like:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>ok</span>({ type: <span>"info"</span>, message: <span>"hello"</span> }) <span>// 💥 throws</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>That may or may not be what you want.</p><hr><h2>3. If you want it precise: brand your error</h2><p>To avoid false positives, make <code>Err</code> uniquely identifiable:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_SYMBOL</span> <span>=</span> <span>Symbol</span>(<span>"Err"</span>)<br><br><span>type</span> <span>Err</span> <span>=</span> {<br>  type: <span>string</span><br>  message: <span>string</span><br>  [<span>ERR_SYMBOL</span>]: <span>true</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Constructor:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>err</span>(<span>e</span>: { type: <span>string</span>; message: <span>string</span> }): <span>Err</span> {<br>  <span>return</span> { ...<span>e</span>, [<span>ERR_SYMBOL</span>]: <span>true</span> }<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Runtime check:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>function</span> <span>isErr</span>(<span>value</span>: <span>any</span>): <span>value</span> <span>is</span> <span>Err</span> {<br>  <span>return</span> <span>!!</span>(<span>value</span> <span>&amp;&amp;</span> <span>value</span>[<span>ERR_SYMBOL</span>])<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Now your <code>ok</code> is safe:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>ok</span>&lt;<span>T</span>&gt;(<span>value</span>: <span>NotErr</span>&lt;<span>T</span>&gt;): <span>T</span> {<br>  <span>if</span> (<span>isErr</span>(<span>value</span>)) {<br>    <span>throw</span> <span>new</span> <span>Error</span>(<span>"ok() received an Err"</span>)<br>  }<br>  <span>return</span> <span>value</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2>Bottom line</h2><ul><li>❌ You cannot inspect <code>T</code> at runtime</li><li>✅ You <em>can</em>:<ul><li>block it at compile time (<code>NotErr&lt;T&gt;</code>)</li><li>enforce it at runtime (shape or brand check)</li></ul></li></ul><p>If you skip the runtime check, someone will eventually sneak an <code>err</code> through (especially via <code>any</code>).</p><hr><p>If you want, I can <span role="button" tabindex="0"><span><svg xmlns="http://www.w3.org/2000/svg" width="20" height="20"></svg></span><span>show how to redesign your API so this problem disappears entirely</span></span> (and you don’t need defensive checks at all).</p></div></div></div></div><div></div></div></div></section><section dir="auto" style="color: rgb(0, 0, 0); white-space-collapse: collapse;"><div><div><div><div dir="auto"><div><div><div>now it works. but i want to let it narrow properly. in rescue if, it must be err. later, it must be ok. i dont want to unwrap anything. dont make ok an object.<pre><code>type ok&lt;T&gt; = Exclude&lt;T, err&gt;
type err = { type: string, message: string }

declare global {
  type result&lt;T&gt; = ok&lt;T&gt; | err;
}

export function ok&lt;T&gt;(value: ok&lt;T&gt;): ok&lt;T&gt; {
  return value
}

const err_prototype = {}

export function err(error: err): err {
  return Object.create(err_prototype, {
    type: {
      value: error.type,
      writable: false,
      enumerable: true
    },
    message: {
      value: error.message,
      writable: false,
      enumerable: true
    }
  })
}

export function rescue&lt;T&gt;(result: result&lt;T&gt;): boolean {
  return result instanceof err
}

function foo(): result&lt;boolean&gt; {
  if (Math.random() &gt; 0.5) {
    return ok(true)
  } else {
    return err({ type: "bad luck", message: "damn" })
  }
}

function test() {
  let bar = foo()

  if (rescue(bar)) {
    console.log(bar);
    return;
  }

  console.log(bar);
}

test()</code></pre></div></div></div></div></div><div></div></div></div></section><section dir="auto" style="color: rgb(0, 0, 0); white-space-collapse: collapse;"><div><div><div><div dir="auto" tabindex="0"><div><div><p>You’re one small step away. The issue now is <strong>not your types</strong>, it’s your <strong>type guard</strong>.</p><p>Right now:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>rescue</span>&lt;<span>T</span>&gt;(<span>result</span>: <span>result</span>&lt;<span>T</span>&gt;): <span>boolean</span> {<br>  <span>return</span> <span>result</span> <span>instanceof</span> <span>err</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>This returns just <code>boolean</code>, so TypeScript <strong>can’t narrow</strong> anything.</p><hr><h2><span role="text">✅ Fix: make <code>rescue</code> a proper type guard</span></h2><p>You need to tell TypeScript:</p><blockquote><p>“If this returns true, then <code>result</code> is <code>err</code>”</p></blockquote><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>rescue</span>&lt;<span>T</span>&gt;(<span>result</span>: <span>result</span>&lt;<span>T</span>&gt;): <span>result</span> <span>is</span> <span>err</span> {<br>  <span>return</span> <span>Object</span><span>.</span>getPrototypeOf(<span>result</span>) <span>===</span> <span>err_prototype</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2><span role="text">❗ Also fix <code>instanceof</code></span></h2><p>This will <strong>never work</strong>:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>result</span> <span>instanceof</span> <span>err</span> <span>// ❌ err is not a class</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>You’re creating objects with <code>err_prototype</code>, so check that directly:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>Object</span><span>.</span>getPrototypeOf(<span>result</span>) <span>===</span> <span>err_prototype</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2>✅ Now narrowing works exactly how you want</h2><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>function</span> <span>test</span>() {<br>  <span>let</span> <span>bar</span> <span>=</span> <span>foo</span>()<br><br>  <span>if</span> (<span>rescue</span>(<span>bar</span>)) {<br>    <span>// ✅ bar is err</span><br>    <span>console</span><span>.</span>log(<span>bar</span><span>.</span>message)<br>    <span>return</span><br>  }<br><br>  <span>// ✅ bar is ok&lt;boolean&gt; → boolean</span><br>  <span>console</span><span>.</span>log(<span>bar</span>)<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>No wrapping, no <code>.value</code>, no unwrapping. <code>ok&lt;T&gt;</code> stays just <code>T</code>.</p><hr><h2>⚠️ One more subtle improvement</h2><p>Your <code>ok</code> function still allows <code>err</code> through via inference edge cases. Tighten it slightly:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>ok</span>&lt;<span>T</span>&gt; <span>=</span> <span>T</span> <span>extends</span> <span>err</span> <span>?</span> <span>never</span> <span>:</span> <span>T</span><br><br><span>export</span> <span>function</span> <span>ok</span>&lt;<span>T</span>&gt;(<span>value</span>: <span>ok</span>&lt;<span>T</span>&gt;): <span>ok</span>&lt;<span>T</span>&gt; {<br>  <span>return</span> <span>value</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2>Final working version (minimal changes)</h2><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>err</span> <span>=</span> { type: <span>string</span>, message: <span>string</span> }<br><span>type</span> <span>ok</span>&lt;<span>T</span>&gt; <span>=</span> <span>T</span> <span>extends</span> <span>err</span> <span>?</span> <span>never</span> <span>:</span> <span>T</span><br><br><span>declare</span> <span>global</span> {<br>  <span>type</span> <span>result</span>&lt;<span>T</span>&gt; <span>=</span> <span>ok</span>&lt;<span>T</span>&gt; <span>|</span> <span>err</span>;<br>}<br><br><span>export</span> <span>function</span> <span>ok</span>&lt;<span>T</span>&gt;(<span>value</span>: <span>ok</span>&lt;<span>T</span>&gt;): <span>ok</span>&lt;<span>T</span>&gt; {<br>  <span>return</span> <span>value</span><br>}<br><br><span>const</span> <span>err_prototype</span> <span>=</span> {}<br><br><span>export</span> <span>function</span> <span>err</span>(<span>error</span>: <span>err</span>): <span>err</span> {<br>  <span>return</span> <span>Object</span><span>.</span>create(<span>err_prototype</span>, {<br>    type: { value: <span>error</span><span>.</span>type, writable: <span>false</span>, enumerable: <span>true</span> },<br>    message: { value: <span>error</span><span>.</span>message, writable: <span>false</span>, enumerable: <span>true</span> }<br>  })<br>}<br><br><span>export</span> <span>function</span> <span>rescue</span>&lt;<span>T</span>&gt;(<span>result</span>: <span>result</span>&lt;<span>T</span>&gt;): <span>result</span> <span>is</span> <span>err</span> {<br>  <span>return</span> <span>Object</span><span>.</span>getPrototypeOf(<span>result</span>) <span>===</span> <span>err_prototype</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2>Reality check (important)</h2><p>This is about as far as you can push this pattern while keeping:</p><ul><li>✅ <code>ok&lt;T&gt;</code> = plain <code>T</code></li><li>✅ no wrappers</li><li>✅ proper narrowing</li></ul><p>But it relies on:</p><ul><li>prototype identity (can break if objects are cloned/serialized)</li><li>structural typing hacks (<code>T extends err ? never : T</code>)</li></ul><p>So it works—but it’s a bit fragile compared to discriminated unions.</p><hr><p>If you want, <span role="button" tabindex="0"><span><svg xmlns="http://www.w3.org/2000/svg" width="20" height="20"></svg></span><span>I can show a version that keeps your “no unwrap” ergonomics but removes the prototype fragility</span></span>.</p></div></div></div></div></div></div></section></body></html>

---

<html><head></head><body><div style="color: rgb(0, 0, 0); white-space-collapse: collapse;"><section dir="auto"><div><div><div><div dir="auto"><div><div><div>how could i fix it. - make ok exclude err - let it log err instead of an empty object<pre><code>type ok&lt;T&gt; = Exclude&lt;T, err&gt;
type err = { type: string, message: string }

declare global {
  type result&lt;T&gt; = ok&lt;T&gt; | err;
}

export function ok&lt;T&gt;(value: T): ok&lt;T&gt; {
  return value
}

const err_prototype = {}

export function err(error: err): err {
  return Object.create(err_prototype, {
    type: {
      value: error.type,
      writable: false,
    },
    message: {
      value: error.message,
      writable: false,
    }
  })
}

export function rescue&lt;T&gt;(result: result&lt;T&gt;): boolean {
  return result instanceof err
}

function foo(): result&lt;boolean&gt; {
  if (Math.random() &gt; 0.5 &amp;&amp; false) {
    return ok(true)
  } else {
    return err({ type: "bad luck", message: "damn" })
  }
}

function test() {
  let bar = foo()

  if (rescue(bar)) {
    console.log(bar);
    return;
  }

  console.log(bar);
}

test()</code></pre></div></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><p>You’re very close, but there are two core problems in your design:</p><ol><li><code>Exclude&lt;T, err&gt;</code> doesn’t actually prevent <code>T</code> from being an <code>err</code></li><li><code>instanceof err</code> can never work because <code>err</code> is <strong>not a class</strong></li></ol><p>Let’s fix both cleanly.</p><hr><h2><span role="text">1. <code>ok&lt;T&gt;</code> doesn’t exclude <code>err</code></span></h2><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>ok</span>&lt;<span>T</span>&gt; <span>=</span> <span>Exclude</span>&lt;<span>T</span>, <span>err</span>&gt;</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>This only removes <code>err</code> <strong>if <code>T</code> is already a union containing it</strong>, which isn’t your case. So it doesn’t protect anything.</p><p>👉 There’s no reliable way to “forbid” a structural type like <code>{ type: string; message: string }</code> in TypeScript.</p><p><strong>Better approach:</strong> don’t fight the type system—use a <strong>tagged union</strong>.</p><hr><h2><span role="text">2. <code>instanceof err</code> is broken</span></h2><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>rescue</span>&lt;<span>T</span>&gt;(<span>result</span>: <span>result</span>&lt;<span>T</span>&gt;): <span>boolean</span> {<br>  <span>return</span> <span>result</span> <span>instanceof</span> <span>err</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p><code>err</code> is a <strong>type + function</strong>, not a constructor. So <code>instanceof</code> always fails.</p><p>Also, your <code>Object.create(err_prototype, …)</code> gives you an object with a custom prototype—but you're not checking against that prototype.</p><hr><h2>Fix: Use a discriminated union (idiomatic + safe)</h2><p>This is the standard pattern and solves everything cleanly.</p><h3>Define types</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>Ok</span>&lt;<span>T</span>&gt; <span>=</span> { ok: <span>true</span>; value: <span>T</span> }<br><span>type</span> <span>Err</span> <span>=</span> { ok: <span>false</span>; type: <span>string</span>; message: <span>string</span> }<br><br><span>type</span> <span>Result</span>&lt;<span>T</span>&gt; <span>=</span> <span>Ok</span>&lt;<span>T</span>&gt; <span>|</span> <span>Err</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h3>Constructors</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>ok</span>&lt;<span>T</span>&gt;(<span>value</span>: <span>T</span>): <span>Ok</span>&lt;<span>T</span>&gt; {<br>  <span>return</span> { ok: <span>true</span>, value }<br>}<br><br><span>export</span> <span>function</span> <span>err</span>(<span>error</span>: { type: <span>string</span>; message: <span>string</span> }): <span>Err</span> {<br>  <span>return</span> { ok: <span>false</span>, ...<span>error</span> }<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h3><span role="text">Type guard (your <code>rescue</code>)</span></h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>isErr</span>&lt;<span>T</span>&gt;(<span>result</span>: <span>Result</span>&lt;<span>T</span>&gt;): <span>result</span> <span>is</span> <span>Err</span> {<br>  <span>return</span> <span>result</span><span>.</span>ok <span>===</span> <span>false</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h3>Usage</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>function</span> <span>foo</span>(): <span>Result</span>&lt;<span>boolean</span>&gt; {<br>  <span>if</span> (<span>Math</span><span>.</span>random() <span>&gt;</span> <span>0.5</span>) {<br>    <span>return</span> <span>ok</span>(<span>true</span>)<br>  } <span>else</span> {<br>    <span>return</span> <span>err</span>({ type: <span>"bad luck"</span>, message: <span>"damn"</span> })<br>  }<br>}<br><br><span>function</span> <span>test</span>() {<br>  <span>const</span> <span>bar</span> <span>=</span> <span>foo</span>()<br><br>  <span>if</span> (<span>isErr</span>(<span>bar</span>)) {<br>    <span>console</span><span>.</span>log(<span>bar</span>) <span>// properly typed as Err</span><br>    <span>return</span><br>  }<br><br>  <span>console</span><span>.</span>log(<span>bar</span><span>.</span>value) <span>// properly typed as boolean</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2><span role="text">Why your original version logged <code>{}</code></span></h2><p>Because:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>console</span><span>.</span>log(<span>bar</span>)</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>When using <code>Object.create</code> with property descriptors, properties are <strong>non-enumerable by default</strong> unless you specify:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>enumerable</span>: <span>true</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>So your object <em>had properties</em>, but <code>console.log</code> didn’t show them.</p><hr><h2>If you really want to keep your prototype approach</h2><p>You’d need:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>err_prototype</span> <span>=</span> {}<br><br><span>export</span> <span>function</span> <span>err</span>(<span>error</span>: <span>err</span>): <span>err</span> {<br>  <span>return</span> <span>Object</span><span>.</span>create(<span>err_prototype</span>, {<br>    type: { value: <span>error</span><span>.</span>type, enumerable: <span>true</span> },<br>    message: { value: <span>error</span><span>.</span>message, enumerable: <span>true</span> }<br>  })<br>}<br><br><span>export</span> <span>function</span> <span>rescue</span>&lt;<span>T</span>&gt;(<span>result</span>: <span>result</span>&lt;<span>T</span>&gt;): <span>result</span> <span>is</span> <span>err</span> {<br>  <span>return</span> <span>Object</span><span>.</span>getPrototypeOf(<span>result</span>) <span>===</span> <span>err_prototype</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>But honestly—this is brittle and unnecessary.</p><hr><h2>Bottom line</h2><ul><li>❌ Don’t use <code>Exclude&lt;T, err&gt;</code> here</li><li>❌ Don’t use <code>instanceof</code> without a class</li><li>❌ Don’t rely on prototype tricks</li><li>✅ Use a <strong>discriminated union (<code>ok: true | false</code>)</strong></li><li>✅ Use a <strong>type guard</strong></li><li>✅ Keep it simple and predictable</li></ul><hr><p>If you want, I can <span role="button" tabindex="0"><span><svg xmlns="http://www.w3.org/2000/svg" width="20" height="20"></svg></span><span>show a version with <strong>pattern matching helpers</strong> (like Rust’s <code>match</code>) to make this even cleaner</span></span>.</p></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><div>why this is brittle and unnecessary</div></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><p>Because you’re building a runtime identity system (via prototypes) to solve a problem TypeScript already solves <em>at compile time</em>—and the two don’t line up cleanly.</p><p>Here’s where it breaks down.</p><hr><h2>1. Structural typing vs prototype identity</h2><p>TypeScript cares about <strong>shape</strong>, not identity:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>Err</span> <span>=</span> { type: <span>string</span>; message: <span>string</span> }</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Any object with that shape is an <code>Err</code>, regardless of how it was created.</p><p>But your runtime check:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>Object</span><span>.</span>getPrototypeOf(<span>result</span>) <span>===</span> <span>err_prototype</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>is checking <strong>identity</strong>, not shape.</p><p>So now you have two competing definitions of “error”:</p><ul><li>TypeScript: “has <code>type</code> + <code>message</code>”</li><li>Runtime: “was created with this exact prototype”</li></ul><p>Those can easily diverge.</p><hr><h2>2. Easy to accidentally break</h2><p>This fails silently in very normal situations:</p><h3>Example: cloning / spreading</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>e1</span> <span>=</span> <span>err</span>({ type: <span>"x"</span>, message: <span>"y"</span> })<br><span>const</span> <span>e2</span> <span>=</span> { ...<span>e1</span> } <span>// loses prototype</span><br><br><span>rescue</span>(<span>e2</span>) <span>// ❌ false</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Even though <code>e2</code> <em>looks identical</em>, your check fails.</p><hr><h3>Example: JSON roundtrip</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>e</span> <span>=</span> <span>err</span>({ type: <span>"x"</span>, message: <span>"y"</span> })<br><span>const</span> <span>parsed</span> <span>=</span> <span>JSON</span><span>.</span>parse(<span>JSON</span><span>.</span>stringify(<span>e</span>))<br><br><span>rescue</span>(<span>parsed</span>) <span>// ❌ false</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>This is a big one—anything crossing a boundary (API, storage, worker) loses the prototype.</p><hr><h3>Example: multiple module instances</h3><p>If your code is bundled or duplicated:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>err_prototype</span> <span>!==</span> <span>err_prototype</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Now your identity check breaks across module boundaries. This happens more often than people expect in real builds.</p><hr><h2><span role="text">3. <code>instanceof</code> doesn’t work (and why that matters)</span></h2><p>You tried:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>result</span> <span>instanceof</span> <span>err</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>That only works with <strong>classes / constructors</strong>, not factory functions.</p><p>You <em>could</em> switch to:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>class</span> <span>Err</span> { ... }</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>But then you’re still tied to prototype identity, with all the same problems above.</p><hr><h2>4. Non-enumerable properties surprise you</h2><p>This bit you already:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>Object</span><span>.</span>create(<span>err_prototype</span>, {<br>  type: { value: ..., writable: <span>false</span> }<br>})</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Defaults to:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>enumerable</span>: <span>false</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>So:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>console</span><span>.</span>log(<span>err</span>(...)) <span>// {}</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>This is the kind of subtle behavior that makes this approach fragile.</p><hr><h2>5. It adds complexity without giving real benefits</h2><p>What are you actually gaining?</p><ul><li>Not performance</li><li>Not better type safety</li><li>Not simpler code</li></ul><p>You’re just introducing:</p><ul><li>hidden state (prototype)</li><li>fragile checks</li><li>debugging confusion</li></ul><hr><h2>6. Discriminated unions solve all of this cleanly</h2><p>With:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>Result</span>&lt;<span>T</span>&gt; <span>=</span><br>  <span>|</span> { ok: <span>true</span>; value: <span>T</span> }<br>  <span>|</span> { ok: <span>false</span>; type: <span>string</span>; message: <span>string</span> }</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Your check becomes:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>result</span><span>.</span>ok <span>===</span> <span>false</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>This is:</p><ul><li>✅ serializable</li><li>✅ stable across boundaries</li><li>✅ works with spreads/clones</li><li>✅ aligned with TypeScript narrowing</li><li>✅ obvious to anyone reading the code</li></ul><hr><h2>The core issue (in one sentence)</h2><p>You’re trying to enforce <strong>nominal typing at runtime</strong> in a language (TypeScript) that is fundamentally <strong>structural</strong>, and that mismatch creates edge cases everywhere.</p><hr><p>If you <em>really</em> want nominal-style safety, there are better patterns (like branded types or symbols), but for a <code>Result</code> type, the discriminated union is the one that scales without surprises.</p></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><div>i must not use the discriminated union. because i have to unwrap it to process it. and ts has a hard limit that does not allow changing the type in the same scope and i dont want to add indentation, yet const data = data wont work even if you try to shadow it in indentation. i dont want data_result and data everytime, which has already caused me troubles. im considering using a magic prop like a hash or err instead of prototype.</div></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><p>You’re solving a real ergonomics problem—but avoiding discriminated unions by switching to prototypes or “magic identity” will create <em>worse</em> ergonomics over time, not better ones.</p><p>Let’s address your core constraint first:</p><blockquote><p>“I don’t want <code>data_result</code> and then <code>data</code>, and I don’t want extra indentation.”</p></blockquote><p>That’s valid. The trick is: <strong>don’t fight narrowing—structure your API so narrowing happens once and the rest is flat.</strong></p><hr><h2>Why “magic prop” beats prototype (but still needs care)</h2><p>If you refuse discriminated unions, then yes—a <strong>sentinel property (preferably a <code>Symbol</code>)</strong> is far more robust than prototypes:</p><h3>Safer than prototype:</h3><ul><li>survives spreads (if you reattach it)</li><li>can be checked cheaply</li><li>doesn’t depend on identity</li><li>works across modules (if symbol is shared)</li></ul><hr><h2>Recommended pattern (minimal friction, no indentation explosion)</h2><h3>1. Use a hidden symbol tag</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR</span> <span>=</span> <span>Symbol</span>(<span>"err"</span>)<br><br><span>type</span> <span>Err</span> <span>=</span> {<br>  [ERR]: <span>true</span><br>  type: <span>string</span><br>  message: <span>string</span><br>}<br><br><span>type</span> <span>Result</span>&lt;<span>T</span>&gt; <span>=</span> <span>T</span> <span>|</span> <span>Err</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h3>2. Constructors</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>ok</span>&lt;<span>T</span>&gt;(<span>value</span>: <span>T</span>): <span>T</span> {<br>  <span>return</span> <span>value</span><br>}<br><br><span>export</span> <span>function</span> <span>err</span>(<span>e</span>: { type: <span>string</span>; message: <span>string</span> }): <span>Err</span> {<br>  <span>return</span> {<br>    [<span>ERR</span>]: <span>true</span>,<br>    ...<span>e</span><br>  }<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h3>3. Type guard</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>isErr</span>&lt;<span>T</span>&gt;(<span>x</span>: <span>Result</span>&lt;<span>T</span>&gt;): <span>x</span> <span>is</span> <span>Err</span> {<br>  <span>return</span> <span>typeof</span> <span>x</span> <span>===</span> <span>"object"</span> <span>&amp;&amp;</span> <span>x</span> <span>!==</span> <span>null</span> <span>&amp;&amp;</span> <span>ERR</span> <span>in</span> <span>x</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2>4. The key: early-return unwrap (no nesting, no renaming)</h2><p>This is what you actually want:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>function</span> <span>test</span>() {<br>  <span>const</span> <span>data</span> <span>=</span> <span>foo</span>()<br><br>  <span>if</span> (<span>isErr</span>(<span>data</span>)) {<br>    <span>console</span><span>.</span>log(<span>data</span>)<br>    <span>return</span><br>  }<br><br>  <span>// from here on, data is T (no rename, no shadowing)</span><br>  <span>console</span><span>.</span>log(<span>data</span>)<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>👉 This already avoids:</p><ul><li>extra variables</li><li>indentation explosion</li><li>shadowing hacks</li></ul><p>TypeScript <strong>does narrow in the same scope after early return</strong>.</p><hr><h2>If TS isn’t narrowing for you</h2><p>Then it’s usually one of these:</p><h3>❌ Problem pattern</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>let</span> <span>data</span> <span>=</span> <span>foo</span>()<br><br><span>if</span> (<span>isErr</span>(<span>data</span>)) {<br>  <span>return</span><br>}<br><br><span>data</span> <span>=</span> <span>transform</span>(<span>data</span>) <span>// ❌ TS may widen again</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><h3>✅ Fix: don’t reassign</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>data</span> <span>=</span> <span>foo</span>()<br><br><span>if</span> (<span>isErr</span>(<span>data</span>)) <span>return</span><br><br><span>const</span> <span>result</span> <span>=</span> <span>transform</span>(<span>data</span>) <span>// clean and safe</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2><span role="text">If you <em>really</em> want zero checks everywhere</span></h2><p>Then what you actually want is a <strong>throwing unwrap helper</strong>:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>unwrap</span>&lt;<span>T</span>&gt;(<span>x</span>: <span>Result</span>&lt;<span>T</span>&gt;): <span>T</span> {<br>  <span>if</span> (<span>isErr</span>(<span>x</span>)) {<br>    <span>console</span><span>.</span>error(<span>x</span>)<br>    <span>throw</span> <span>new</span> <span>Error</span>(<span>x</span><span>.</span>message)<br>  }<br>  <span>return</span> <span>x</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Usage:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>data</span> <span>=</span> <span>unwrap</span>(<span>foo</span>())<br><span>// data is T immediately</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>No unions leaking. No renaming. No indentation.</p><hr><h2>Why symbol tag &gt; string tag &gt; prototype</h2><h3>Symbol (best)</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div>[<span>ERR</span>]: <span>true</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><ul><li>no collision risk</li><li>not accidentally serialized</li><li>explicit intent</li></ul><h3>String (ok but weaker)</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>__err</span>: <span>true</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><ul><li>can collide</li><li>can be faked easily</li></ul><h3>Prototype (worst here)</h3><ul><li>breaks on spread / JSON / boundaries</li><li>identity issues</li><li>harder to reason about</li></ul><hr><h2>Bottom line</h2><ul><li>Your instinct to avoid <code>data_result</code> / <code>data</code> churn is <strong>correct</strong></li><li>Prototype-based identity is the wrong tool</li><li>A <strong>symbol-tagged union + early return OR unwrap()</strong> gives you:<ul><li>flat code</li><li>no renaming</li><li>no nesting</li><li>reliable runtime behavior</li></ul></li></ul><hr><p>If you want, I can <span role="button" tabindex="0"><span><svg xmlns="http://www.w3.org/2000/svg" width="20" height="20"></svg></span><span>show a version that gives you <strong>Rust-style <code>.map()</code>, <code>.andThen()</code>, <code>.unwrapOr()</code></strong> without ever manually checking <code>isErr</code></span></span>.</p></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><div>i think a hash is better than a symbol for serialization</div></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><p>You’re right about one thing: a <code>Symbol</code> won’t survive serialization. But switching to a “hash” (string tag) isn’t automatically <em>better</em>—it just trades one set of problems for another.</p><p>Let’s break it down so you can choose intentionally.</p><hr><h2>What you actually need</h2><p>You want a value that:</p><ol><li>Survives JSON / transport</li><li>Can be reliably detected at runtime</li><li>Doesn’t ruin ergonomics</li></ol><p>That’s a <strong>serialization concern</strong>, not a type-system one.</p><hr><h2>Symbol vs string (“hash”)</h2><h3>Symbol</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div>[<span>ERR</span>]: <span>true</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><ul><li>✅ Impossible to collide with user data</li><li>✅ Great for in-memory safety</li><li>❌ Lost in JSON.stringify</li><li>❌ Not transferable across boundaries</li></ul><hr><h3>String tag (“hash”)</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div>{ <span>__err</span>: <span>true</span> }</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><ul><li>✅ Survives JSON</li><li>✅ Easy to check</li><li>❌ Can collide or be spoofed</li><li>❌ Slightly “magic string”-y</li></ul><hr><h2>The important insight</h2><p>If your data crosses boundaries (API, storage, workers), <strong>you already can’t trust runtime identity anyway</strong>.</p><p>So worrying about “spoofing” is mostly theoretical unless you’re doing security-sensitive work.</p><hr><h2>Practical, robust approach</h2><p>Use a <strong>string tag that is unlikely to collide + explicit shape</strong>.</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_TAG</span> <span>=</span> <span>"__myapp_err__"</span><br><br><span>type</span> <span>Err</span> <span>=</span> {<br>  [ERR_TAG]: <span>true</span><br>  type: <span>string</span><br>  message: <span>string</span><br>}<br><br><span>type</span> <span>Result</span>&lt;<span>T</span>&gt; <span>=</span> <span>T</span> <span>|</span> <span>Err</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h3>Constructor</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>err</span>(<span>e</span>: { type: <span>string</span>; message: <span>string</span> }): <span>Err</span> {<br>  <span>return</span> {<br>    [<span>ERR_TAG</span>]: <span>true</span>,<br>    ...<span>e</span><br>  }<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h3>Type guard</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>export</span> <span>function</span> <span>isErr</span>&lt;<span>T</span>&gt;(<span>x</span>: <span>Result</span>&lt;<span>T</span>&gt;): <span>x</span> <span>is</span> <span>Err</span> {<br>  <span>return</span> (<br>    <span>typeof</span> <span>x</span> <span>===</span> <span>"object"</span> <span>&amp;&amp;</span><br>    <span>x</span> <span>!==</span> <span>null</span> <span>&amp;&amp;</span><br>    (<span>x</span> <span>as</span> <span>any</span>)[<span>ERR_TAG</span>] <span>===</span> <span>true</span><br>  )<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2>Why this is better than a “hash”</h2><p>If by <em>hash</em> you mean something like:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>hash</span>: <span>"a94f3c..."</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>that’s actually worse:</p><ul><li>You gain nothing over a simple tag</li><li>You make debugging harder</li><li>You still need a known constant to compare against</li><li>It doesn’t prevent spoofing anyway</li></ul><p>So:</p><blockquote><p>A “hash” is just a more complicated magic string.</p></blockquote><hr><h2>If you want stronger guarantees</h2><p>You can combine <strong>tag + shape validation</strong>:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>function</span> <span>isErr</span>(<span>x</span>: <span>unknown</span>): <span>x</span> <span>is</span> <span>Err</span> {<br>  <span>return</span> (<br>    <span>typeof</span> <span>x</span> <span>===</span> <span>"object"</span> <span>&amp;&amp;</span><br>    <span>x</span> <span>!==</span> <span>null</span> <span>&amp;&amp;</span><br>    (<span>x</span> <span>as</span> <span>any</span>)[<span>ERR_TAG</span>] <span>===</span> <span>true</span> <span>&amp;&amp;</span><br>    <span>typeof</span> (<span>x</span> <span>as</span> <span>any</span>)<span>.</span>type <span>===</span> <span>"string"</span> <span>&amp;&amp;</span><br>    <span>typeof</span> (<span>x</span> <span>as</span> <span>any</span>)<span>.</span>message <span>===</span> <span>"string"</span><br>  )<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Now random objects won’t accidentally pass.</p><hr><h2>Best of both worlds (advanced)</h2><p>If you want:</p><ul><li>safe in-memory</li><li>safe over the wire</li></ul><p>You can dual-tag:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_SYMBOL</span> <span>=</span> <span>Symbol</span>(<span>"err"</span>)<br><span>const</span> <span>ERR_TAG</span> <span>=</span> <span>"__err__"</span><br><br><span>type</span> <span>Err</span> <span>=</span> {<br>  [ERR_SYMBOL]: <span>true</span><br>  [<span>ERR_TAG</span>]: <span>true</span><br>  type: <span>string</span><br>  message: <span>string</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><ul><li>Symbol → runtime safety</li><li>String → serialization</li></ul><hr><h2>Bottom line</h2><ul><li>✔ Yes, use a string tag if you need serialization</li><li>❌ Don’t bother with hashes—they don’t add real safety</li><li>✔ Keep it simple, explicit, and debuggable</li></ul><hr><p>If you want, I can help you design a version where you <strong>never manually check <code>isErr</code> at all</strong>, while still keeping your “no renaming, no nesting” constraint.</p></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><div>i mean i use my own symbol function using sha256</div></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><p>Ah, got it—you’re generating a deterministic “symbol” via SHA-256 so it survives serialization.</p><p>That <em>sounds</em> stronger, but in practice it doesn’t buy you anything over a simple constant string, and it adds a few downsides.</p><hr><h2>The key misconception</h2><p>A hash feels “unique” and “secure”, but in your case:</p><blockquote><p>You’re not verifying integrity—you’re just tagging a value.</p></blockquote><p>So this:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>hash</span>: <span>sha256</span>(<span>"err"</span>)</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>and this:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>__err</span>: <span>true</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>are functionally equivalent for detection.</p><p>Because at runtime you still do:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>x</span><span>.</span>hash <span>===</span> <span>EXPECTED_HASH</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Which is no different than:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>x</span><span>.</span>__err <span>===</span> <span>true</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2>Why SHA-256 is unnecessary here</h2><h3>1. No real security gain</h3><p>Anyone (or any bug) that can produce:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div>{ <span>hash</span>: <span>EXPECTED_HASH</span> }</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>could just as easily produce:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div>{ <span>__err</span>: <span>true</span> }</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>You’re not preventing spoofing—just obscuring the tag.</p><hr><h3>2. Worse ergonomics</h3><p>Debugging:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div>{ <span>hash</span>: <span>"a3f9c2e8b7..."</span> }</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>vs</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div>{ <span>__err</span>: <span>true</span> }</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>One is immediately obvious, the other isn’t.</p><hr><h3>3. More complexity for zero benefit</h3><ul><li>You need a hashing function</li><li>You need to ensure consistency across environments</li><li>You still rely on a shared constant</li></ul><hr><h3>4. It doesn’t solve the real edge cases</h3><p>Hash or not, this still breaks the same way:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>e</span> <span>=</span> <span>err</span>(...)<br><span>const</span> <span>cloned</span> <span>=</span> { ...<span>e</span> }<br><br><span>isErr</span>(<span>cloned</span>) <span>// still depends entirely on the tag</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>So the <em>mechanism</em> is identical.</p><hr><h2><span role="text">When a hash <em>would</em> make sense</span></h2><p>Only if you were doing something like:</p><ul><li>verifying data integrity from untrusted sources</li><li>preventing tampering</li><li>cryptographic validation</li></ul><p>That’s not what this pattern is for.</p><hr><h2>What actually works best here</h2><p>Use a <strong>clear, namespaced string tag</strong>:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_TAG</span> <span>=</span> <span>"__myapp_result_err__"</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>That already gives you:</p><ul><li>practically zero collision risk</li><li>perfect readability</li><li>no runtime cost</li><li>full serialization support</li></ul><hr><h2>If you want extra rigor (without hashes)</h2><p>Add shape checking:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>function</span> <span>isErr</span>(<span>x</span>: <span>unknown</span>): <span>x</span> <span>is</span> <span>Err</span> {<br>  <span>return</span> (<br>    <span>typeof</span> <span>x</span> <span>===</span> <span>"object"</span> <span>&amp;&amp;</span><br>    <span>x</span> <span>!==</span> <span>null</span> <span>&amp;&amp;</span><br>    (<span>x</span> <span>as</span> <span>any</span>)<span>.</span>__myapp_result_err__ <span>===</span> <span>true</span> <span>&amp;&amp;</span><br>    <span>typeof</span> (<span>x</span> <span>as</span> <span>any</span>)<span>.</span>type <span>===</span> <span>"string"</span> <span>&amp;&amp;</span><br>    <span>typeof</span> (<span>x</span> <span>as</span> <span>any</span>)<span>.</span>message <span>===</span> <span>"string"</span><br>  )<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>That’s <strong>more meaningful safety</strong> than a hash.</p><hr><h2>Bottom line</h2><ul><li>A SHA-256 tag is just a <strong>fancy constant</strong></li><li>It doesn’t meaningfully improve correctness or safety here</li><li>It <em>does</em> make debugging and maintenance worse</li></ul><p>So if your goal is:</p><blockquote><p>“robust + serializable + ergonomic”</p></blockquote><p>Then a <strong>well-named string tag + type guard</strong> is the sweet spot.</p><hr><p>If you want, <span role="button" tabindex="0"><span><svg xmlns="http://www.w3.org/2000/svg" width="20" height="20"></svg></span><span>I can show a pattern that completely removes the need for <code>isErr</code> checks while keeping your “no nesting / no renaming” rule intact</span></span>.</p></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><div>no. i mean the key. __err might collide. the hash is my own.</div></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><p>Ah—now we’re talking about <strong>key collision</strong>, not value spoofing. That’s a much more reasonable concern.</p><p>You’re right that something like:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div>{ <span>__err</span>: <span>true</span> }</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p><em>can</em> collide with user data in theory. But using a SHA-256-derived key is still overkill for that problem.</p><hr><h2>The important distinction</h2><p>You don’t need a key that is:</p><ul><li>cryptographically secure</li><li>globally unique across adversaries</li></ul><p>You just need a key that is:</p><blockquote><p><strong>practically impossible to collide with accidentally in your codebase</strong></p></blockquote><hr><h2>Why a hash is still not the best tradeoff</h2><p>Even if you generate something like:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_TAG</span> <span>=</span> <span>"a94f3c6b1e9d4..."</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>you get:</p><h3>Downsides</h3><ul><li>unreadable in logs / debugging</li><li>harder to grep/search</li><li>no real increase in <em>practical</em> safety</li><li>still just a shared constant (same trust model)</li></ul><hr><h2>What actually solves collision cleanly</h2><p>Use a <strong>namespaced, absurdly-specific string key</strong>.</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_TAG</span> <span>=</span> <span>"__myapp_result_err_v1__"</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>or even:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_TAG</span> <span>=</span> <span>"__internal_result_error_do_not_touch__"</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><h3>Why this works</h3><ul><li>Collisions only happen if <em>you</em> create them</li><li>External data rarely uses weird double-underscore namespaced keys</li><li>It’s readable and debuggable</li><li>Zero runtime cost</li></ul><p>In practice, this is what most production systems do.</p><hr><h2><span role="text">If you want <em>stronger-than-string</em> without hashes</span></h2><p>You can combine strategies:</p><h3>1. Namespaced key + shape</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_TAG</span> <span>=</span> <span>"__myapp_result_err__"</span><br><br><span>function</span> <span>isErr</span>(<span>x</span>: <span>unknown</span>): <span>x</span> <span>is</span> <span>Err</span> {<br>  <span>return</span> (<br>    <span>typeof</span> <span>x</span> <span>===</span> <span>"object"</span> <span>&amp;&amp;</span><br>    <span>x</span> <span>!==</span> <span>null</span> <span>&amp;&amp;</span><br>    (<span>x</span> <span>as</span> <span>any</span>)[<span>ERR_TAG</span>] <span>===</span> <span>true</span> <span>&amp;&amp;</span><br>    <span>typeof</span> (<span>x</span> <span>as</span> <span>any</span>)<span>.</span>type <span>===</span> <span>"string"</span> <span>&amp;&amp;</span><br>    <span>typeof</span> (<span>x</span> <span>as</span> <span>any</span>)<span>.</span>message <span>===</span> <span>"string"</span><br>  )<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Now accidental collision is extremely unlikely.</p><hr><h3>2. Optional: dual-layer (symbol + string)</h3><p>If you want maximum rigor:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_SYMBOL</span> <span>=</span> <span>Symbol</span>(<span>"err"</span>)<br><span>const</span> <span>ERR_TAG</span> <span>=</span> <span>"__myapp_result_err__"</span><br><br><span>type</span> <span>Err</span> <span>=</span> {<br>  [ERR_SYMBOL]: <span>true</span><br>  [<span>ERR_TAG</span>]: <span>true</span><br>  type: <span>string</span><br>  message: <span>string</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><ul><li>Symbol → safe in-memory</li><li>String → survives serialization</li></ul><hr><h2><span role="text">When your hash approach <em>does</em> make sense</span></h2><p>Your SHA-256 key is justified if:</p><ul><li>you’re designing a <strong>public protocol</strong></li><li>or consuming <strong>untrusted external JSON</strong></li><li>or need <strong>versioned, globally unique markers</strong></li></ul><p>Otherwise, it’s solving a problem that realistically won’t occur.</p><hr><h2>The pragmatic takeaway</h2><ul><li>✔ Your instinct (avoid collisions) is valid</li><li>❌ A hash is overengineering for this layer</li><li>✔ A <strong>namespaced string key + shape check</strong> is the sweet spot</li><li>✔ Add a symbol if you want extra in-memory guarantees</li></ul><hr><p>If you want, I can help you design a version that:</p><ul><li>keeps your flat control flow</li><li>avoids renaming variables</li><li>and gives you <strong>safe chaining (map/flatMap)</strong> without ever touching the tag manually.</li></ul></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><div>it does solve the edge cases. i just check the hash. not the prototype anymore. not the props.</div></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><p>You’re not wrong that switching from prototype → hash key removes a bunch of the brittleness. It <strong>does</strong> fix the biggest issues you had (prototype loss, <code>instanceof</code>, enumerability, etc.).</p><p>But the important pushback is this:</p><blockquote><p>It doesn’t actually solve <em>more</em> edge cases than a normal string tag—it just obscures the same mechanism.</p></blockquote><hr><h2>What your hash actually guarantees</h2><p>If you do:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_KEY</span> <span>=</span> <span>sha256</span>(<span>"my-secret-namespace"</span>)<br><br><span>type</span> <span>Err</span> <span>=</span> {<br>  [ERR_KEY]: <span>true</span><br>  type: <span>string</span><br>  message: <span>string</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>and check:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>if</span> (<span>x</span>?.[<span>ERR_KEY</span>] <span>===</span> <span>true</span>)</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Then yes:</p><ul><li>✅ survives JSON</li><li>✅ survives spread</li><li>✅ no prototype issues</li><li>✅ extremely low chance of <em>accidental</em> key collision</li></ul><p>So far, solid.</p><hr><h2>But here’s the key point</h2><p>A hash key and a long string key behave the same in all <em>practical</em> edge cases.</p><p>These two are equivalent:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_KEY</span> <span>=</span> <span>"a94f3c6b1e..."</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_KEY</span> <span>=</span> <span>"__myapp_result_err__"</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>Because:</p><ul><li>Both rely on <strong>shared knowledge of the key</strong></li><li>Both can be copied if someone really wants to</li><li>Both survive serialization the same way</li><li>Both fail or succeed identically under spread / cloning</li></ul><hr><h2>The only thing the hash improves</h2><p>This is the <em>one</em> real benefit:</p><blockquote><p>It reduces the chance of <strong>accidental collision from external or unknown data</strong>.</p></blockquote><p>That’s it.</p><p>If that’s your goal, then yes—your approach is valid.</p><hr><h2><span role="text">Where it <em>doesn’t</em> help (important)</span></h2><p>Even with a hash:</p><h3>1. Spoofing is still trivial</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div>{ [<span>ERR_KEY</span>]: <span>true</span> }</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>If someone has the key, they can fake it. Same as any string.</p><hr><h3>2. You still need shape checks (if you care about correctness)</h3><p>Otherwise this passes:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div>{ [<span>ERR_KEY</span>]: <span>true</span> }</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>But it’s not a real error.</p><hr><h3>3. Debugging gets worse</h3><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div>{<br>  <span>"a94f3c6b1e9d4..."</span>: <span>true</span>,<br>  <span>type</span>: <span>"bad"</span>,<br>  <span>message</span>: <span>"oops"</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>vs</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div>{<br>  <span>__myapp_result_err__</span>: <span>true</span>,<br>  <span>type</span>: <span>"bad"</span>,<br>  <span>message</span>: <span>"oops"</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>One is self-explanatory, the other isn’t.</p><hr><h2>So is your approach “wrong”?</h2><p>No. It’s actually <strong>perfectly reasonable</strong> <em>if</em>:</p><ul><li>you care about serialized data boundaries</li><li>you want near-zero collision risk</li><li>you’re okay with worse readability</li></ul><hr><h2>The honest tradeoff</h2><div><div tabindex="-1"><table><thead><tr><th>Approach</th><th>Collision risk</th><th>Debuggability</th><th>Complexity</th></tr></thead><tbody><tr><td>Simple string tag</td><td>already tiny</td><td>excellent</td><td>minimal</td></tr><tr><td>Namespaced string</td><td>negligible</td><td>excellent</td><td>minimal</td></tr><tr><td>SHA-256 key</td><td>ultra-tiny</td><td>poor</td><td>higher</td></tr></tbody></table></div></div><hr><h2>A cleaner version of what you're doing</h2><p>If you stick with it, at least make it intentional:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_KEY</span> <span>=</span> <span>"a94f3c6b1e9d4..."</span> <span>as</span> <span>const</span><br><br><span>type</span> <span>Err</span> <span>=</span> {<br>  [ERR_KEY]: <span>true</span><br>  type: <span>string</span><br>  message: <span>string</span><br>}<br><br><span>type</span> <span>Result</span>&lt;<span>T</span>&gt; <span>=</span> <span>T</span> <span>|</span> <span>Err</span><br><br><span>export</span> <span>function</span> <span>isErr</span>&lt;<span>T</span>&gt;(<span>x</span>: <span>Result</span>&lt;<span>T</span>&gt;): <span>x</span> <span>is</span> <span>Err</span> {<br>  <span>return</span> (<br>    <span>typeof</span> <span>x</span> <span>===</span> <span>"object"</span> <span>&amp;&amp;</span><br>    <span>x</span> <span>!==</span> <span>null</span> <span>&amp;&amp;</span><br>    (<span>x</span> <span>as</span> <span>any</span>)[<span>ERR_KEY</span>] <span>===</span> <span>true</span><br>  )<br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><hr><h2>Bottom line</h2><ul><li>You <em>did</em> fix the real brittleness (prototype issues)</li><li>The hash gives you <strong>stronger collision resistance</strong>, but…</li><li>It doesn’t fundamentally change how the system behaves</li><li>It’s a <strong>tradeoff: safety margin vs clarity</strong></li></ul><p>If that tradeoff matters in your system (e.g., lots of unknown JSON inputs), your approach is justified.</p><p>If it’s just internal app logic, it’s probably more complexity than you need.</p></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto"><div><div><div>no one but the result library could touch the private hash. and you will never touch the hash either, it's shown as the var in ide.</div></div></div></div></div><div></div></div></div></section><section dir="auto"><div><div><div><div dir="auto" tabindex="0"><div><div><p>Got it—you’re treating the hash as a <strong>private, unforgeable key</strong> exposed only through the library API. That <em>does</em> tighten things up compared to a plain <code>"__err"</code> string.</p><p>But there’s one subtle reality to keep in mind:</p><blockquote><p>It’s not actually private at runtime—just <em>conventionally hidden</em>.</p></blockquote><hr><h2>Where your reasoning holds</h2><p>If you do something like:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>const</span> <span>ERR_KEY</span> <span>=</span> <span>sha256</span>(<span>"my-lib/internal/err"</span>) <span>as</span> <span>unique</span> <span>symbol</span><span>-</span><span>like string</span></div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>and never export it, then:</p><ul><li>Consumers won’t accidentally collide with it ✅</li><li>It won’t show up in IntelliSense as something to use ✅</li><li>Your <code>isErr()</code> becomes the only “official” way to detect errors ✅</li></ul><p>So ergonomically and architecturally, this is <strong>perfectly sound</strong>.</p><hr><h2><span role="text">The one thing you <em>can’t</em> guarantee</span></h2><p>Even if it’s not exported, the key still exists at runtime:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>Object</span><span>.</span>keys(<span>obj</span>)<br><span>Object</span><span>.</span>getOwnPropertySymbols(<span>obj</span>)<br><span>Reflect</span><span>.</span>ownKeys(<span>obj</span>)</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><p>So technically:</p><ul><li>It <em>can</em> be discovered</li><li>It <em>can</em> be copied</li><li>It’s not cryptographically hidden</li></ul><p>But this only matters if you’re defending against <strong>malicious code</strong>, which you almost certainly aren’t in this pattern.</p><hr><h2>So the real question is: what problem are you solving?</h2><p>Your design now gives you:</p><ul><li>No prototype brittleness ✅</li><li>No accidental collisions ✅</li><li>Serialization-safe identity ✅</li><li>Flat control flow (your original goal) ✅</li></ul><p>That’s already a solid system.</p><hr><h2>The only meaningful tradeoff left</h2><p>You’ve optimized for:</p><blockquote><p>“Make this impossible to misuse accidentally”</p></blockquote><p>At the cost of:</p><ul><li>Less transparent debugging</li><li>Slightly more mental overhead when inspecting raw data</li></ul><p>That’s a fair trade if your library is reused widely or crosses boundaries.</p><hr><h2>One small suggestion (worth considering)</h2><p>Even if you keep the hash key, you can improve debuggability <strong>without weakening your guarantees</strong>:</p><pre><div><div><div><div><div><div><div><div><div><div></div></div></div><div><div><div><div dir="ltr"><div><div><span>type</span> <span>Err</span> <span>=</span> {<br>  [ERR_KEY]: <span>true</span><br>  type: <span>string</span><br>  message: <span>string</span><br>  __tag?: <span>"Err"</span> <span>// purely for humans/logging</span><br>}</div></div></div></div></div></div></div></div></div></div><div><div></div></div></div></div></div></pre><ul><li>Your logic still relies <strong>only on the hash</strong></li><li>But logs / console output become readable</li></ul><hr><h2>Bottom line</h2><p>You’re no longer in the “brittle” territory from before.</p><ul><li>Prototype approach → genuinely fragile ❌</li><li>Hash-key tagging → <strong>robust and consistent</strong> ✅</li></ul><p>At this point, it’s not about correctness anymore—it’s about <strong>ergonomics vs opacity</strong>, and you’ve made a defensible choice.</p><p>If your priority is a clean API and zero accidental misuse, your approach holds up.</p></div></div></div></div></div></div></section></div></body></html>
